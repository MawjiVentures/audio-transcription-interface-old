{
  "name": "audio-transcription-interface",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-h5-audio-player": "^1.0.1",
    "react-player": "^1.6.4",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "aws s3 sync --acl public-read --delete build/ s3://intefaces-audio-transcription",
    "invalidate-distribution": "aws cloudfront create-invalidation --distribution-id E8BLGNZHN97AK --paths /index.html /\\* "
  }
}
